---
const { date } = Astro.props;
const dateFormatted = new Date(date).toISOString();
const dateLocale = new Date(date).toLocaleDateString("de-de", {
  year: "numeric",
  month: "short",
  day: "numeric",
});
---

<time datetime={dateFormatted}>
  {dateLocale}
</time>
